import { PreviewContainerDirective } from './preview-container.directive';

describe('PreviewContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new PreviewContainerDirective();
    expect(directive).toBeTruthy();
  });
});
